# Question routing

Use this map to choose what to read first.

## If the user asks about startup

Read:

1. `../../generated/startup-and-routing.md`
2. source repo `src/main.ts`
3. source repo `src/App.vue`

## If the user asks about routing

Read:

1. `../../generated/startup-and-routing.md`
2. source repo `src/framework/router/route.ts`
3. source repo `src/framework/router/index.ts`
4. source repo `src/framework/router/backEnd.ts`

## If the user asks about a business module

Read:

1. `../../generated/module-index.md`
2. matching `src/biz/views/...`
3. matching `src/biz/api/...`

## If the user asks where to edit something

Read in order:

1. target page `index.vue`
2. `modules/` folder
3. matching API file
4. framework hooks/components if needed

## If the user asks for a learning plan

Read:

1. `../../docs/newcomer-learning-path.md`
2. `../../generated/project-overview.md`
3. `../../generated/startup-and-routing.md`
